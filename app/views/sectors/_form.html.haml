= form_for(sector) do |f|
  - if sector.errors.any?
    #error_explanation
      %h2
        = pluralize(sector.errors.count, "error")
        prohibited this sector from being saved:
      %ul
        - sector.errors.full_messages.each do |message|
          %li= message
  .field
    = f.label :name
    %br/
    = f.text_field :name
  .actions
    = f.submit
